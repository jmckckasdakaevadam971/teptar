'use client';

/**
 * Заглушка экрана модерации (MVP — этап 1).
 * Здесь будет очередь записей со статусом 'pending' для роли teip_admin:
 * список изменений, кнопки «Одобрить»/«Отклонить», фильтр по тейпу/селу.
 * Эндпоинты планируются: GET /api/moderation/queue, POST /api/moderation/:id/approve.
 */
export default function AdminPage() {
  return (
    <div className="card">
      <h1>Модерация</h1>
      <p style={{ color: '#64748b' }}>
        Раздел для администраторов тейпов. В MVP здесь появится очередь правок,
        ожидающих подтверждения, с кнопками «Одобрить» и «Отклонить».
      </p>
      <p>Статус: запланировано (этап 1, см. docs/ROADMAP.md).</p>
    </div>
  );
}
